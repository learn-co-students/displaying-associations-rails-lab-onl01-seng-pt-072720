<h1>Artists</h1>
<ul>
    <% @artists.each do |artist| %>
        <li>
            <%= artist.name %><br>
            <%= artist.song_count %> <%= "song".pluralize(artist.song_count) %><br>
        </li>
    <% end %>
</ul>