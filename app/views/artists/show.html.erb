<h1><%= @artist.name %></h1>
    <ul>
<% @artist.songs.each do |s| %>
        <li><%= link_to s.title, song_path(s) %></li>
<% end %>
    </ul>

<h1>Artist Song Count: <%= @artist.song_count %> song</h1>